# Copy to .env in the project root:
#   cp .env.prod.example .env

# Required
SECRET_KEY=replace-with-openssl-rand-hex-32
DOMAIN=dialog.example.com
LETSENCRYPT_EMAIL=admin@example.com

# Optional
ACCESS_TOKEN_EXPIRE_MINUTES=10080
HTTP_PORT=80
HTTPS_PORT=443
